<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.god.dream.dao.IUserDao">

    <resultMap id="userMap" type="com.god.dream.model.User">
        <id property="id" column="id"/>

        <result property="name" column="name"/>
        <result property="password" column="password"/>
    </resultMap>

    <select id="find" parameterType="long" resultMap="userMap">
        select id, name, password from t_user where id = #{0}
    </select>

    <select id="lookupUsersByName" parameterType="string" resultMap="userMap">
        select id, name, password from t_user where name = #{0}
    </select>

</mapper>
